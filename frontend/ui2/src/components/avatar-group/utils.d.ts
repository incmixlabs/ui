import type { AvatarProps } from "@/components/radixui/avatar";
export declare const getVisibleCount: (users: AvatarProps[], maxVisible: number) => {
    visibleCount: number;
    remainingCount: number;
};
//# sourceMappingURL=utils.d.ts.map